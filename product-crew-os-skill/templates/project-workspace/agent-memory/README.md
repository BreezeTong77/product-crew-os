# Agent Memory / 角色项目记忆

This folder stores project-scoped role memory summaries.

Do not store raw sub-agent chat logs by default. Store compressed memory deltas with source references, confidence, and scope.
